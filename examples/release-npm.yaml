name: Release

on:
  push:
    tags:
      - "v*"

permissions: {}

jobs:
  release:
    uses: luxass/shared-workflows/.github/workflows/release-npm.yaml@v0.4.4 # x-release-please-version
    secrets: inherit
